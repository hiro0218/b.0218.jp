name: Auto Approve PR based on Labels

on:
  pull_request:
    types: [labeled]

jobs:
  auto-approve:
    runs-on: ubuntu-slim
    permissions:
      pull-requests: write

    steps:
      - name: Check for auto-approve label
        id: check-label
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo, number } = context.issue;
            const sender = context.payload.sender?.login;
            const { data: pullRequest } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: number,
            });

            const labels = pullRequest.labels.map(label => label.name);
            console.log('PR Labels:', labels);

            // 承認対象となるラベル
            const approveLabels = ['auto-approve'];

            // ラベルの存在チェック
            const hasApproveLabel = labels.some(label => approveLabels.includes(label));
            console.log('Has approve label:', hasApproveLabel);

            const isAuthorizedActor = sender === owner;
            console.log('Authorized actor:', isAuthorizedActor);

            return hasApproveLabel && isAuthorizedActor;

      - name: Approve Pull Request
        if: ${{ steps.check-label.outputs.result == 'true' }}
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo, number } = context.issue;

            // PRを承認
            await github.rest.pulls.createReview({
              owner,
              repo,
              pull_number: number,
              event: 'APPROVE',
              body: 'Approved!'
            });

            console.log('PR has been automatically approved');

import { styled } from '@/ui/styled';

export const Container = styled.div`
  inline-size: 100%;
  max-inline-size: calc(var(--container-width) + var(--space-3) * 2);
  padding-inline: var(--space-3);
  margin-inline: auto;
`;

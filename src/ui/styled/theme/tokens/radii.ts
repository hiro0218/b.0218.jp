import type { TokenValues } from './types';

const radiiValues: TokenValues<'radii'> = {
  xs: { value: '2px' },
  sm: { value: '4px' },
  md: { value: '8px' },
  lg: { value: '12px' },
  full: { value: '100%' },
};

export default radiiValues;

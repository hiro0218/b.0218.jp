import type { TokenValues } from './types';

const zIndex: TokenValues<'zIndex'> = {
  base: { value: 1 },
  header: { value: 10 },
  overlay: { value: 11 },
  search: { value: 12 },
};

export default zIndex;

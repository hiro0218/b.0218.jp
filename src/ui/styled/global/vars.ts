const globalVars = {
  '--container-width': '800px',
  '--transition-fast': 'var(--durations-fast) var(--easings-ease-out-expo)',
  '--transition-normal': 'var(--durations-normal) var(--easings-ease-out-expo)',
  '--transition-slow': 'var(--durations-slow) var(--easings-ease-out-expo)',
};

export default globalVars;
